// print 1(true) or 0(false)
//  a. if it's sunday and it's snowing --> true
#include <stdio.h>
int main()
{
    int isSunday = 1;
    int isSnowing = 1;
    printf("%d\n", isSunday && isSnowing);
    return 0;
}