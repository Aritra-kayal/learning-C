// print 1(true) or 0(false)
//  b. if it's monday or it's raining --> true
#include <stdio.h>
int main()
{
    int isMonday = 1;
    int isRaining = 0;
    printf("%d\n", isMonday || isRaining);
    return 0;
}